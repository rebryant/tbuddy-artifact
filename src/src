This directory contains sources for the following:

buddy: The BuDDy BDD library and the TBUDDY trusted BDD library

tbsat: A proof-generating SAT solver that uses the TBUDDY library

lrat-check: A proof checker for the proofs generated by tbsat

The archive files are copied to ../lib
The header files are copied to ../include
The executables are copied to ../bin
